// TODO: Implement
// Statement Mapping
/**
 * Returns a statement key and respective values
 *  
 * @param {Object} testResult 
 * @param {number} index 
 * @param {number} testCounter 
 * @returns {object} statement and s combined as an object
 */
 export default function addstatementnode(testResult, index, testCounter){
	const statementMap = {
	}
	const s = {
	}
	return {statementMap, s}
}